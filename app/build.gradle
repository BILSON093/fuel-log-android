plugins {
    id "com.android.application"
}

android {
    namespace "com.codex.fuellog"
    compileSdk 35

    defaultConfig {
        applicationId "com.codex.fuellog"
        minSdk 26
        targetSdk 35
        versionCode 2
        versionName "1.0.1"
    }
}

dependencies {
    implementation fileTree(dir: "libs", include: ["*.aar"])
}
